# ── Core ──────────────────────────────────────────────────────────────────────
DJANGO_SECRET_KEY=CHANGE_ME_generate_with_python_-c_"import_secrets;print(secrets.token_urlsafe(64))"
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
SITE_URL=https://yourdomain.com

# ── Database ───────────────────────────────────────────────────────────────────
USE_SQLITE=False
POSTGRES_DB=projeto_raven
POSTGRES_USER=raven
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ── Redis ─────────────────────────────────────────────────────────────────────
# Redis roda na porta 6380 (não padrão) para não conflitar com Redis do app existente
REDIS_PASSWORD=CHANGE_ME_redis_password
REDIS_URL=redis://:CHANGE_ME_redis_password@redis:6380/1
CELERY_BROKER_URL=redis://:CHANGE_ME_redis_password@redis:6380/2
CHANNEL_LAYER_URL=redis://:CHANGE_ME_redis_password@redis:6380/3

# ── Portas expostas no host (para o Cloudflare Tunnel rotear) ────────────────
# Ajuste se já houver outro serviço nessas portas no servidor
FRONTEND_PORT=3100   # Cloudflare: yourdomain.com → http://localhost:3100
DJANGO_PORT=8100     # Cloudflare: yourdomain.com/api/ → http://localhost:8100 (opcional)

# ── CORS / CSRF ────────────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=https://yourdomain.com
CSRF_TRUSTED_ORIGINS=https://yourdomain.com

# ── Email ──────────────────────────────────────────────────────────────────────
# Configure via the admin panel (SMTP Settings) or set here:
# EMAIL_HOST=smtp.yourdomain.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=noreply@yourdomain.com
# EMAIL_HOST_PASSWORD=CHANGE_ME
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=Projeto Raven <noreply@yourdomain.com>
EMAIL_SETTINGS_ENCRYPTION_SALT=CHANGE_ME_random_salt_32_chars

# ── OAuth Google ──────────────────────────────────────────────────────────────
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=

# ── Monitoring ────────────────────────────────────────────────────────────────
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.05
METRICS_ENABLED=False

# ── Rate limiting ─────────────────────────────────────────────────────────────
REST_THROTTLING_ENABLED=True
REST_THROTTLE_ANON=100/min
REST_THROTTLE_USER=300/min
REST_THROTTLE_LOGIN=10/min
REST_THROTTLE_REGISTER=3/hour
REST_THROTTLE_PASSWORD_RESET=3/hour
REST_THROTTLE_OTP=5/minute
REST_THROTTLE_FORUM_POST=20/minute
REST_THROTTLE_BLOG_POST=10/minute
REST_THROTTLE_COMMENT=10/minute

# ── App versioning (injected by CI/CD) ────────────────────────────────────────
APP_VERSION=
APP_BUILD_SHA=
APP_BUILD_TIME=

# ── Misc ──────────────────────────────────────────────────────────────────────
DJANGO_LOG_LEVEL=WARNING
LOG_FORMAT=json
CREATE_DEFAULT_SUPPORT_USER=False
